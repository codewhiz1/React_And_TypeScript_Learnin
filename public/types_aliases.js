"use strict";
const logDetails = (uid, item) => {
    console.log(`${item} has a uid of ${uid}`);
};
const newDetails = (user) => {
    console.log(`${user.names_12} says hello`);
};
const newDetails_12 = (user) => {
    console.log(`${user.names_12} says hello !!!`);
};
